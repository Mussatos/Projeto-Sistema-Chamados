import { BrowserRouter, Link } from "react-router-dom";
import RoutesApp from '../src/routes';

function App() {
  return (
    <BrowserRouter>
      <RoutesApp />
    </BrowserRouter>
  );
}

export default App;
